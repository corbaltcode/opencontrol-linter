# frozen_string_literal: true

source 'https://rubygems.org'

# dependencies are in the .gemspec file
gemspec

local_gemfile = 'Gemfile.local'
eval_gemfile local_gemfile if File.exist?(local_gemfile)
